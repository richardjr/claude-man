"""Curated packs (Phase 6) — skills + CLAUDE.md fragments selected per project.

``library.py`` is the PURE read side (discover/parse the in-repo library); ``materialize.py``
writes a project's selection into its asset source for the existing ``assets.sync_in`` rail to
carry into the container binds. Design: docs/PACKS.md.
"""
